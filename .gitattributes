# Treat as text when is possible and ensure Unix line-endings.
* text=auto eol=lf

# Ignore differences on Yarn's lockfile.
# Since version 1.0, Yarn automatically handle merge conflicts.
yarn.lock -diff
